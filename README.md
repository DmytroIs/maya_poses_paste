# maya_poses_paste
maya py batch tool, that saves the delta between the base and desired poses, and propogates it to the folder of .ma files. The purpose of it is to update the set of .ma animations with new poses. The tool recognizes file names for up to 4 prefixes and does some manipulation on rig constraints. 
